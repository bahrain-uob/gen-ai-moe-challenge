import { Button } from '../components/Button';

const Exercises = () => (
  <>
    <div className="flex">
      If you're looking for links, please checkout the new{' '}
      <Button to="/dev"> /dev </Button>
    </div>
  </>
);

export default Exercises;
